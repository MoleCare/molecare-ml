# MoleCare-ML runtime dependencies
# Target: Python 3.12
#
# DIRECT dependencies only. constraints.txt holds security floors for
# transitive packages; requirements.lock holds the fully pinned, resolved set
# used by CI and the container images. Regenerate the lock with:
#
#   uv pip compile requirements.txt -c constraints.txt -o requirements.lock \
#       --python-version 3.12 --universal --no-header

# --- ML / inference ---------------------------------------------------------
tensorflow>=2.21.0
keras>=3.15.1
# The bundled Xception artefact is a Keras 2.9 SavedModel, which Keras 3 cannot
# load. tf-keras is the maintained Keras 2 compatibility package and is required
# until the model is re-exported to the Keras 3 .keras format.
tf-keras>=2.21.0
numpy>=2.5.2
scikit-learn>=1.9.0
opencv-python-headless>=5.0.0.93,<6.0.0
Pillow>=12.3.0

# --- Derm Foundation (Google Health AI) -------------------------------------
huggingface_hub>=1.28.0

# --- Web serving ------------------------------------------------------------
Flask>=3.1.3
flask-cors>=6.0.5
Werkzeug>=3.1.8
gunicorn>=26.2.0

# --- MLOps ------------------------------------------------------------------
metaflow>=2.19.39
wandb>=0.28.2

# --- AWS --------------------------------------------------------------------
boto3>=1.43.81

# --- Testing ----------------------------------------------------------------
pytest>=9.1.1
pytest-flask>=1.3.0
pytest-cov>=7.1.0
